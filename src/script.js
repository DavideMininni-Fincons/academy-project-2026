/**
 * Exercise notes
 *
 * Open the style.css file.
 *
 * The client wants the banner to appear floating in the exact center of the screen (both horizontally and vertically),
 * overlapping everything else. Use the position and transform properties to center it perfectly.
 *
 * The close button must be positioned top-right and its style needs to be improved too.
 *
 * The list and the button must be styled too, but you CAN'T change the HTML; use advanced selectors.
 */